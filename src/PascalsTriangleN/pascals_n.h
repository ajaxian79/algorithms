//
// Created by ajaxian on 07/04/20.
//

#ifndef ALGORITHMS_PASCALS_N_H
#define ALGORITHMS_PASCALS_N_H

// Build the first `num_rows` rows of Pascal's triangle. Returns a heap-
// allocated array of int* (each row also heap-allocated). *return_size is
// set to num_rows; *col_sizes points to a heap array with each row's length.
int** pascals_triangle_n(int num_rows, int* return_size, int** col_sizes);

#endif //ALGORITHMS_PASCALS_N_H
