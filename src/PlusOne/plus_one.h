//
// Created by ajaxian on 02/10/24.
//

#ifndef ALGORITHMS_PLUS_ONE_H
#define ALGORITHMS_PLUS_ONE_H

// Treats `digits` (most-significant-first) as a non-negative big integer
// and returns digits+1 as a freshly malloc'd array. *return_size is set
// to the result's length. Caller frees.
int* plus_one(int* digits, int digits_size, int* return_size);

#endif //ALGORITHMS_PLUS_ONE_H
