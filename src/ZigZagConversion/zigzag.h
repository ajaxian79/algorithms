//
// Created by ajaxian on 05/18/24.
//

#ifndef ALGORITHMS_ZIGZAG_H
#define ALGORITHMS_ZIGZAG_H

// Read `s` row-by-row after writing it in a zigzag with `num_rows` rows.
// Returns a heap string (caller frees). Returns a duplicate of `s` if
// num_rows <= 1.
char* zigzag_convert(const char* s, int num_rows);

#endif //ALGORITHMS_ZIGZAG_H
